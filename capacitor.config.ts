import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.smptamhar.portal',
  appName: 'Portal SMP Taman Harapan Bekasi',
  webDir: 'dist'
};

export default config;
